import Form from "./Form"

const Aparece = ({login}) =>(
    <div>
        <Form login={login}/>
    </div>
)
// se puede trabajar de esta manera con parentesis en lugar de llaves por que no hay logica y asi evitamos el retorno

export default Aparece